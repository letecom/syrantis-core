export function Settings() {
  return (
    <section className="page-panel">
      <p className="eyebrow">Workspace</p>
      <h2>Settings placeholder</h2>
      <p>
        Settings are reserved for later scoped issues. Auth, tenants, secrets, and production
        configuration are not part of this shell.
      </p>
    </section>
  );
}
